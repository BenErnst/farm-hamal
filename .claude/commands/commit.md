Stage all tracked changes with `git add -u`, then run `git diff --cached` to understand what changed. Also check if `CLAUDE.md` has been modified (it may be untracked or unstaged); if so, stage it too without asking the user. Before committing, print the list of staged files to the terminal (run `git diff --cached --name-only`). Write a concise commit message in imperative mood (subject ≤72 chars; add a short body only if the change needs explanation). Commit with that message, then push to the current remote branch. Show git status before and after. Do not use --no-verify or skip hooks.
